package com.inferrix.Sorting.controller;

import java.util.Arrays;

import org.springframework.http.HttpEntity;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpMethod;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.client.RestTemplate;

// not useable
public class RestClient {
	/*
	 * private static final String url =
	 * "http://localhost:8080/stackInstance/allArticles"; static RestTemplate
	 * restTemplate = new RestTemplate();
	 * 
	 * public static void main(String[] args) { callAllArticles(); }
	 * 
	 * 
	 * private static ResponseEntity<String> callAllArticles() { HttpHeaders headers
	 * = new HttpHeaders();
	 * headers.setAccept(Arrays.asList(MediaType.APPLICATION_JSON));
	 * HttpEntity<String> entity = new HttpEntity<>("parameters", headers);
	 * ResponseEntity<String> result = restTemplate.exchange(url,HttpMethod.GET,
	 * entity, String.class); return result; }
	 */

}
